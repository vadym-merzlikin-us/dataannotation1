# Host ports published by docker-compose.
# 8080/4200/5432 are deliberately avoided: the brighton2 stack already uses them.
BACKEND_PORT=8090
FRONTEND_PORT=4600
DB_PORT=5433

# Local development credentials only - not production secrets.
POSTGRES_DB=codejunk
POSTGRES_USER=codejunk
POSTGRES_PASSWORD=codejunk
